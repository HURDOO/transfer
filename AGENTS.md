# Project Working Rules

## 기준 문서

- 사용자의 원래 의도와 제약은 `PROJECT_BRIEF.md`에서 확인한다.
- 합의된 제품 요구사항과 완료 조건의 기준은 `docs/SPEC.md`이다.
- 기술 구조와 데이터 흐름의 기준은 `docs/ARCHITECTURE.md`이다.
- 현재 구현 순서와 상태는 `docs/TASKS.md`에서 관리한다.
- 되돌리기 어렵거나 프로젝트 전반에 영향을 주는 선택은 `docs/DECISIONS.md`에 기록한다.
- 문서끼리 또는 문서와 구현이 충돌하면 임의로 숨기지 말고 충돌을 명시한다.

## 작업 시작

- 코드를 수정하기 전에 관련 문서, 기존 구현, 실행 및 테스트 명령을 확인한다.
- 큰 기능은 먼저 명세와 작업 목록을 검토하고 한 번에 하나의 작업 단위만 구현한다.
- 브리프만 있고 명세가 비어 있다면 코딩 전에 명시된 요구사항과 가정을 구분해 문서를 작성한다.
- 합리적인 가정으로 진행할 수 없는 제품 결정만 사용자에게 질문한다.

## 구현

- 기존 코딩 스타일과 프로젝트 구조를 우선한다.
- 공유 타입과 API 계약을 먼저 확정한다.
- 요청되지 않은 미래 기능이나 사용되지 않는 추상화를 만들지 않는다.
- 새 의존성은 필요성과 유지보수 비용을 확인한 뒤 추가한다.
- 비밀값은 환경변수로 관리하고 저장소에 커밋하지 않는다.

## 검증

- 변경 후 프로젝트가 제공하는 lint, typecheck, test, build를 실행한다.
- 버그 수정에는 가능한 범위에서 회귀 테스트를 추가한다.
- 프론트엔드 변경은 실제 브라우저에서 반응형, 오류 상태, 빈 상태를 확인한다.
- 임시 코드, 디버그 출력, 사용하지 않는 파일을 제거한다.
- 관련 검증이 실패한 상태를 완료로 표시하지 않는다.

## 완료 보고

- 구현한 동작과 변경한 주요 파일을 요약한다.
- 실행한 검증과 결과를 적는다.
- 실행하지 못한 검증, 남은 위험, 수동 확인 사항을 분명히 남긴다.
- 의미 있는 작업 단위가 끝나면 `docs/TASKS.md` 상태를 갱신한다.

## 프로젝트 범위와 스택

- 이 프로젝트는 `옮기기` 웹 SPA와 동일 origin Fastify API를 한 pnpm 패키지로 관리한다.
- 클라이언트는 React 19, TypeScript, Vite 8과 프로젝트 CSS를 사용한다.
- 서버는 Node.js 24.15 이상, Fastify 5와 Node 내장 `node:sqlite` 기반 SQLite3를 사용한다.
- 메타데이터는 `STORAGE_DIR/transfer.sqlite3`, 파일 본문은 `STORAGE_DIR/files/`에 두며 `data/`는 커밋하지 않는다.
- 현재 업로드는 로컬 익명 흐름이다. 업로드 인증과 디스크 quota를 결정하기 전에는 공개 인터넷에 노출하지 않는다.

## 실제 명령

- 개발: `pnpm dev` (`http://127.0.0.1:5173`)
- 전체 검증: `pnpm check`
- 개별 검증: `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`
- production 로컬 실행: `pnpm build && pnpm start`
